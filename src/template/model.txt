<?php

namespace {{ project.NameSpace }}\{{ model.FileType.NameSpace }};
{% if model.name == 'User' %}
use Illuminate\Notifications\Notifiable;
use Illuminate\Foundation\Auth\User as Authenticatable;
{% else %}
use Illuminate\Database\Eloquent\Model;
{% endif %}
/**
 * class {{ model.name }}
 * {% for field in entity.FieldManager.list %}
 * @property {{ field.phpType }} ${{ field.name }}{% endfor %}
 */
class {{ model.name }} extends {% if model.name == 'User' %}Authenticatable{% else %}Model{% endif %}
{
{% if model.name == 'User' %}    use Notifiable;
{% endif %}
    /**
     * The table associated with the model.
     *
     * @var string
     */
    protected $table = '{{ entity.tableName }}';

    /**
     * The primary key for the model.
     *
     * @var string
     */
    protected $primaryKey = '{{ entity.primaryKey }}';

    /**
     * Indicates if the IDs are auto-incrementing.
     *
     * @var bool
     */
    public $incrementing = {{ entity.FieldManager.hasIncrement }};

    /**
     * The attributes that are mass assignable.
     *
     * @var array
     */
    protected $fillable = [
{% for field in entity.FieldManager.fillableList %}
        '{{ field.name }}',
{% endfor %}
    ];

    /**
     * The attributes that should be hidden for arrays.
     *
     * @var array
     */
    protected $hidden = [
{% for field in entity.FieldManager.hiddenList %}
        '{{ field.name }}',
{% endfor %}
    ];

    /**
     * The attributes that should be cast to native types.
     *
     * @var array
     */
    protected $casts = [
{% for field in entity.FieldManager.castList %}
        '{{ field.name }}' => '{{ field.cast }}',
{% endfor %}
    ];

    /**
     * Indicates if the model should be timestamped.
     *
     * @var bool
     */
    public $timestamps = {{ entity.FieldManager.hasCreateAndUpdateTimeStamp }};

{% for relation in entity.RelationManager.list %}
    public function {{ relation.name }}()
    {
        return {{ relation.text }};
    }
{% endfor %}
}
